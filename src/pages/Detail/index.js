import React from 'react';
import { View, Text } from 'react-native';
import styles from './styles';

export default () => {
    return(
        <View>
            <Text>
                Hello Word, from detail
            </Text>
        </View>
    );
};